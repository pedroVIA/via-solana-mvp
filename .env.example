# Via Labs V4 Message Gateway - Environment Configuration v2.0.0
# =============================================================================
# IMPORTANT: Never commit actual values! Copy this to .env and fill in values
# =============================================================================

# =============================================================================
# NETWORK CONFIGURATION (REQUIRED)
# =============================================================================
# Network name - Valid: localnet, devnet, testnet, mainnet-beta
NETWORK=devnet

# RPC endpoint - REQUIRED by Anchor for all operations
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com

# =============================================================================
# KEYPAIR CONFIGURATION (REQUIRED)
# =============================================================================
# Wallet keypair for signing transactions - REQUIRED by Anchor
# Use absolute paths or ~ for home directory in production
ANCHOR_WALLET=./keypairs/authority.json

# Program keypair for deployment (only needed for initial deployment)
# After deployment, use PROGRAM_ID instead
PROGRAM_KEYPAIR=./keypairs/program.json


# =============================================================================
# TRANSACTION CONFIGURATION
# =============================================================================
# Commitment level - Valid: processed, confirmed, finalized (default: confirmed)
COMMITMENT_LEVEL=confirmed

# Skip preflight checks - Valid: true, false (default: false)
# Only set to true if you know what you're doing
SKIP_PREFLIGHT=false

# Transaction retry configuration
MAX_RETRIES=3
RETRY_DELAY_MS=1000

# Compute budget configuration (default: 200000)
COMPUTE_BUDGET=200000

# Priority fee in lamports (default: 0, increase for faster processing)
# Devnet recommendation: 5000-10000 for priority
PRIORITY_FEE_LAMPORTS=5000

# =============================================================================
# OPERATIONAL CONFIGURATION
# =============================================================================
# Log level - Valid: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Health check interval in milliseconds (default: 60000)
HEALTH_CHECK_INTERVAL_MS=60000

# Monitoring and alerting (optional but recommended for production)
METRICS_ENABLED=false
METRICS_PORT=9090
ALERT_WEBHOOK_URL=

# =============================================================================
# NETWORK PRESETS - Alternative configurations
# =============================================================================

# -----------------------------------------------------------------------------
# LOCALNET (Local Development) - When you need local testing
# -----------------------------------------------------------------------------
# NETWORK=localnet
# ANCHOR_PROVIDER_URL=http://127.0.0.1:8899
# WS_URL=ws://127.0.0.1:8900
# ANCHOR_WALLET=./keypairs/authority.json
# PROGRAM_KEYPAIR_PATH=./keypairs/program.json
# PROGRAM_ID=
# COMMITMENT_LEVEL=processed
# LOG_LEVEL=debug
# PRIORITY_FEE_LAMPORTS=0

# -----------------------------------------------------------------------------
# TESTNET (Staging)
# -----------------------------------------------------------------------------
# NETWORK=testnet
# ANCHOR_PROVIDER_URL=https://api.testnet.solana.com
# WS_URL=wss://api.testnet.solana.com
# ANCHOR_WALLET=~/.config/solana/testnet-wallet.json
# PROGRAM_ID=
# COMMITMENT_LEVEL=confirmed
# LOG_LEVEL=info
# PRIORITY_FEE_LAMPORTS=10000

# -----------------------------------------------------------------------------
# MAINNET-BETA (Production - USE WITH EXTREME CAUTION!)
# -----------------------------------------------------------------------------
# NETWORK=mainnet-beta
# ANCHOR_PROVIDER_URL=https://your-private-rpc-provider.com
# RPC_FALLBACK_URL=https://api.mainnet-beta.solana.com
# WS_URL=wss://your-private-rpc-provider.com
# ANCHOR_WALLET=~/.config/solana/mainnet-wallet.json
# PROGRAM_ID=
# COMMITMENT_LEVEL=finalized
# LOG_LEVEL=warn
# SKIP_PREFLIGHT=false
# MAX_RETRIES=5
# RETRY_DELAY_MS=2000
# COMPUTE_BUDGET=300000
# PRIORITY_FEE_LAMPORTS=50000
# METRICS_ENABLED=true
# METRICS_PORT=9090
# HEALTH_CHECK_INTERVAL_MS=30000

# =============================================================================
# VALIDATION REFERENCE
# =============================================================================
# NETWORK:           localnet | devnet | testnet | mainnet-beta
# COMMITMENT_LEVEL:  processed (fast) | confirmed (recommended) | finalized (safest)
# LOG_LEVEL:         debug (verbose) | info (normal) | warn (important) | error (critical)
# SKIP_PREFLIGHT:    true | false (always use false unless debugging)
# METRICS_ENABLED:   true | false
#
# File paths can be:
#   Relative:  ./keypairs/wallet.json (from project root)
#   Absolute:  /home/user/wallets/wallet.json
#   Home:      ~/wallets/wallet.json or ~/.config/solana/wallet.json
#
# URLs must include protocol:
#   HTTP/HTTPS: http://localhost:8899 or https://api.devnet.solana.com
#   WebSocket:  ws://localhost:8900 or wss://api.devnet.solana.com
#
# Priority Fees (lamports):
#   0:      No priority (may be slow)
#   1000:   Low priority
#   5000:   Medium priority (recommended for devnet)
#   10000:  High priority
#   50000+: Maximum priority (mainnet production)